## java类加载机制

Bootstrap ClassLoader  启动类加载器
         |
         |
   ExtClassLoader      扩展类加载器
         |
         |
   AppClassLoader      应用类加载器

双亲委派模型：
  如果一个类加载器收到了类加载的请求，它首先不会自己去尝试加载这个类，而是把请求委托给父加载器去完成，依次向上，因此，所有的类加载请求最终都应该被传递到顶层的启动类加载器中，只有当父加载器在它的搜索范围中没有找到所需的类时，即无法完成该加载，子加载器才会尝试自己去加载该类。缓存机制：类被加载之后，都会缓存到缓存内，所以每次对类做出修改，都需要重新加载该类。
 
